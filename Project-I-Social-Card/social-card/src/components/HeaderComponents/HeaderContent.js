import React from 'react';
import './Header.css';

const HeaderContent= () => {
    return (
      <div className="HeaderContent">
       <p className="content"> Let's learn React by building simple interfaces
       with components. Don't try to overthink it, just keep it simple and have fun.
       Once you are comfertable using components you are well on your way to to mastering
       React!</p>
      </div>
    );
  };


export default HeaderContent;